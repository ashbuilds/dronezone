# _Drone zone for zen with admin control_
Steps: 
```
React build :

$ npm install

dev server ->
$ npm run dev

build client ->
$ npm run build

PHP server :

cd server composer install

$ php index.php start
```
